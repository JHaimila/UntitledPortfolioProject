using UnityEngine;

namespace RPG.Control.EnemyController
{
    public class EnemyLookingState : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}

